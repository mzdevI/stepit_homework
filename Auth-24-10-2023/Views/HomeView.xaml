<Page x:Class="Auth_24_10_2023.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Auth_24_10_2023.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="White"
      Title="HomeView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="8,8,8,0" Background="AliceBlue">
            <Button Click="LogoutButton_Click" HorizontalAlignment="Right" Margin="2" Style="{StaticResource MaterialDesignOutlinedButton}">Logout</Button>
            <DockPanel Grid.Row="0">
                <Label VerticalAlignment="Center" HorizontalAlignment="Left">Welcome</Label>
                <Label x:Name="EmailNameLabel" VerticalAlignment="Center" HorizontalAlignment="Left"></Label>
            </DockPanel>
        </Grid>
    </Grid>
</Page>
